# 编码规范与风格指南 (Coding Style)

### 1. 核心布局规范 (PEP 8 基础)
* **缩进：** 使用 **4 个空格** 进行缩进，严禁使用制表符 (Tab)。
* **行宽：** 每行代码建议不超过 **120 个字符**。长的表达式建议使用圆括号进行隐式续行，而不是使用反斜杠 `\`。
* **空行：**
* 顶层函数和类之间空 **2 行**。
* 类内部的方法之间空 **1 行**。
* **导入 (Imports)：** 应该放在文件顶部。顺序建议：
1. 标准库导入
2. 第三方库导入
3. 本地库/自定义模块导入

### 2. 命名约定
Python 的命名应具有描述性，遵循以下惯例：
* **类名 (Classes)：** 使用 `PascalCase`（大驼峰命名法），如 `MyClass`。
* **函数名与变量名：** 使用 `snake_case`（小写加下划线），如 `calculate_total_price()`。
* **常量：** 使用 `UPPER_CASE`（全大写加下划线），如 `MAX_RETRIES = 5`。
* **受保护的成员：** 在名称前加单下划线，如 `_internal_function()`。

### 3. 代码风格与可读性
* **运算符间距：** 二元运算符（`=`, `+=`, `-`, `==`, `>`, `and` 等）两侧各保留一个空格。
* **括号使用：** 函数调用、列表、字典的括号周围**不要**加额外空格。例如：`func(arg1, arg2)` 而非 `func( arg1, arg2 )`。
* **避免冗余：** 不要显式地将布尔值与 `True` 或 `False` 比较。例如，直接写 `if is_active:` 而不是 `if is_active == True:`。
* **分号：** 不要使用分号将多条语句放在同一行，也不要在行尾加分号。

### 4. 注释与风格
- 错误处理：优先使用 `try...catch`，禁止使用空的 catch 块
- 我不喜欢cli添加参数,为此请将默认配置参数写入的脚本中
- 不要搞什么保守回退,凡是没有执行文件,错误的或者旧版本兼容历史的路径分支一律报错
- 注释中不要出现noqa之类的东西,只填写有意义的注释
- 鼓励代码简洁,不要过度设计,不需要的功能和代码请删除,不需要的注释也请删除
- 只鼓励脚本中必要的简洁中文注释
- 代码的虚拟运行环境沙盒外的:conda activate tread


